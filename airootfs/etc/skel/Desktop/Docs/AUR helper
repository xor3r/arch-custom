**************************************************
* Install YAY for searching in user repositories *
**************************************************

All commands to type into terminal are written after $ sign.
Open up terminal and run following commands in order to install YAY - AUR helper:

$ cd ~/
$ git clone https://aur.archlinux.org/yay.git
$ cd yay/
$ makepkg -si
